import Title from "./Title";

const Navbar = ({ title }) => {
  return (
    <section className="navbar">
      <Title title="HIOF" />
      <ul>
        <li>User</li>
        <li>User</li>
      </ul>
    </section>
  );
};

export default Navbar;
